# Inventario Fashion Team

Este es el sistema de inventario para Fashion Team.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Set up your environment variables in `.env`
3. Run the app:
   `npm run dev`
